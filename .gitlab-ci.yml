image: node:8

before_script:
  - npm install

cache:
  paths:
    - node_modules/

test:
  script:
    - npm run coverage

publish:
  script:
    - npm publish
  when: manual
  dependencies:
    - test
  only:
    - master
